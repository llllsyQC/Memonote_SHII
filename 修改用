' ================================
' 📦 Module: modLogUtility
' ローカルログ出力 + エラー回復支援（Step対応）
' ================================

Public gLogFilePath As String
Public gHasErrorOccurred As Boolean

' 📌 初期化：ログファイル準備（ローカルに保存）
Sub InitLogger(yyyymm As String)
    Dim folderPath As String
    folderPath = Environ("USERPROFILE") & "\Documents\工数ログ\" & Left(yyyymm, 4) & "\" & Right(yyyymm, 2)
    
    MkDirRecursive folderPath
    
    gLogFilePath = folderPath & "\log_" & Format(Now, "yyyymmdd_HHmmss") & ".txt"
    
    Open gLogFilePath For Output As #1
    Print #1, "【ログ開始】" & Now
    Close #1
End Sub

' 📌 ステップ付きログ追記
Sub WriteLogStep(stepCode As String, msg As String)
    On Error Resume Next
    Open gLogFilePath For Append As #1
    Print #1, "[" & Format(Now, "yyyy/mm/dd HH:MM:SS") & "] " & stepCode & " ▶ " & msg
    Close #1
End Sub

' 📌 通常ログ追記（補助的に使用可）
Sub WriteLog(msg As String)
    On Error Resume Next
    Open gLogFilePath For Append As #1
    Print #1, "[" & Format(Now, "yyyy/mm/dd HH:MM:SS") & "] " & msg
    Close #1
End Sub

' 📌 フォルダ存在チェック + 再帰作成
Sub MkDirRecursive(ByVal path As String)
    Dim tempPath As String, folders() As String
    Dim i As Integer
    folders = Split(path, "\")
    tempPath = folders(0)
    For i = 1 To UBound(folders)
        tempPath = tempPath & "\" & folders(i)
        If Dir(tempPath, vbDirectory) = "" Then MkDir tempPath
    Next i
End Sub

' 📌 グローバルロールバック処理
Sub ErrorRollback(Optional ByVal stepCode As String = "", Optional ByVal errMsg As String = "")
    gHasErrorOccurred = True
    If errMsg <> "" Then
        If stepCode <> "" Then
            WriteLogStep stepCode, "❌ エラー発生：" & errMsg
        Else
            WriteLog "❌ エラー発生：" & errMsg
        End If
    End If
    MsgBox "エラーが発生しました。" & vbCrLf & errMsg & vbCrLf & "ログを確認してください。", vbCritical, "処理中断"
End Sub

We are writing to inform you that a strong odor was detected on June 19 in Machine Room 7A.

We received a report from Mr. Maurin stating that a stench was coming from Machine Room 7A (SR01) at approximately 9:52 AM.
Our building technician entered the room between 10:56 and 11:38 AM for investigation, accompanied by Mr. Cunanan.

Following the investigation, we identified the humidifier as the source of the odor. The air filter element required replacement.

Subsequently, three building technicians entered Machine Room 7A (SR01) and completed the replacement work by 11:38 AM.

The humidifier is now operating normally without any noticeable odor.

*Please note that these technicians were originally scheduled to enter the room for today’s monthly maintenance. Therefore, no additional entry records are required for this investigation.

We apologize for any inconvenience this may have caused.