Option Explicit

' ====== 設定 ======
Private Const WATCH_RANGE As String = "E4:E1000"   ' 監視対象範囲
Private Const COL_H As Long = 8                    ' H列

' 色設定（仕様固定）
Private Const CLR_INFO_R As Long = 221, CLR_INFO_G As Long = 235, CLR_INFO_B As Long = 247 ' 空白行：薄青
Private Const CLR_WARN_R As Long = 255, CLR_WARN_G As Long = 255, CLR_WARN_B As Long = 0   ' 警告：黄色（不足/無在庫）
Private Const CLR_EQUAL_R As Long = 146, CLR_EQUAL_G As Long = 208, CLR_EQUAL_B As Long = 80 ' 一致：緑（E=H）

Private Sub Worksheet_Change(ByVal Target As Range)
    Dim c As Range, r As Long
    Dim valE As Double, valH As Double
    Dim i As Long
    
    On Error GoTo exit_handler
    If Intersect(Target, Me.Range(WATCH_RANGE)) Is Nothing Then Exit Sub
    
    Application.EnableEvents = False
    
    For Each c In Intersect(Target, Me.Range(WATCH_RANGE))
        r = c.Row
        
        ' 空白：E薄青、D～G無色
        If Trim$(c.Value) = "" Then
            c.Interior.Color = RGB(CLR_INFO_R, CLR_INFO_G, CLR_INFO_B)
            For i = 4 To 7: Me.Cells(r, i).Interior.ColorIndex = xlNone: Next
            GoTo next_cell
        Else
            c.Interior.ColorIndex = xlNone
        End If
        
        ' D～Gを初期化（無色）
        For i = 4 To 7: Me.Cells(r, i).Interior.ColorIndex = xlNone: Next
        
        ' --- 特殊行：在庫の有無のみ（0/1） ---
        If IsSpecialRow(r) Then
            If Len(Trim$(c.Value)) > 0 Then
                If IsNumeric(c.Value) Then
                    valE = CDbl(c.Value)
                    Select Case valE
                        Case 0
                            ' 無在庫 → 黄色
                            For i = 4 To 7
                                Me.Cells(r, i).Interior.Color = RGB(CLR_WARN_R, CLR_WARN_G, CLR_WARN_B)
                            Next
                        Case 1
                            ' 在庫あり → 無色（既にクリア）
                        Case Else
                            ' 0/1 以外 → 入力エラー表示
                            c.Value = "入力エラー"
                    End Select
                Else
                    c.Value = "入力エラー"
                End If
            End If
            GoTo next_cell
        End If
        
        ' --- 非特殊行：E と H を比較 ---
        If IsNumeric(c.Value) Then valE = CDbl(c.Value) Else valE = 0
        valH = GetHNumericValue(r)
        
        For i = 4 To 7
            With Me.Cells(r, i).Interior
                If valE < valH Then
                    .Color = RGB(CLR_WARN_R, CLR_WARN_G, CLR_WARN_B)      ' 不足：黄色
                ElseIf valE = valH Then
                    .Color = RGB(CLR_EQUAL_R, CLR_EQUAL_G, CLR_EQUAL_B)    ' 一致：緑
                Else
                    .ColorIndex = xlNone                                   ' 過剰：無色
                End If
            End With
        Next
        
next_cell:
    Next c
    
exit_handler:
    Application.EnableEvents = True
End Sub

' ====== ユーティリティ ======

' 特殊行判定（名前付き範囲「SpecialRows」から行番号を取得）
Private Function IsSpecialRow(ByVal r As Long) As Boolean
    Dim rng As Range, cell As Range, v
    On Error GoTo no_name
    Set rng = Range("SpecialRows") ' 例：設定シートのA列等
    On Error GoTo 0
    
    If rng Is Nothing Then Exit Function
    
    For Each cell In rng.Cells
        v = Trim$(CStr(cell.Value))
        If Len(v) > 0 Then
            If IsNumeric(v) Then
                If CLng(v) = r Then
                    IsSpecialRow = True
                    Exit Function
                End If
            End If
        End If
    Next
    Exit Function
no_name:
    ' 名前が未定義なら False（= 全て通常行）
    IsSpecialRow = False
End Function

' H列の比較用数値を取得（結合セル対応・文字列から最初の数値を抽出）
Private Function GetHNumericValue(ByVal r As Long) As Double
    Dim hCell As Range, txt As String, m As Object
    Set hCell = Me.Cells(r, COL_H)
    If hCell.MergeCells Then Set hCell = hCell.MergeArea(1, 1) ' 結合セルの左上を参照
    
    If IsNumeric(hCell.Value) And Len(hCell.Value) > 0 Then
        GetHNumericValue = CDbl(hCell.Value)
    Else
        txt = CStr(hCell.Value)
        With CreateObject("VBScript.RegExp")
            .Pattern = "(\d+)"
            .Global = False
            .IgnoreCase = True
            If .Test(txt) Then
                Set m = .Execute(txt)
                GetHNumericValue = CDbl(m(0).Value)
            Else
                GetHNumericValue = 0
            End If
        End With
    End If
End Function